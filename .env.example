# Required: Your Anthropic API key for Claude
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Optional: NCBI API key for higher rate limits (10/sec vs 3/sec)
# Register at: https://www.ncbi.nlm.nih.gov/account/settings/
NCBI_API_KEY=

# Optional: Protects /api/analytics endpoints. If empty, analytics are open (convenient for local dev).
ANALYTICS_API_KEY=

# Optional: Application settings
DEBUG=false

# Optional: SQLite database path (relative to backend working directory)
DATABASE_PATH=data/brobiotic.db
